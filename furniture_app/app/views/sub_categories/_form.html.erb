<%= form_with(model: sub_category) do |form| %>
  <% if sub_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
        <% sub_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cat_id, style: "display: block" %>
    <%= form.number_field :cat_id %>
  </div>

  <div>
    <%= form.label :subcat_name, style: "display: block" %>
    <%= form.text_field :subcat_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
